# Railway config: build and run Next.js app from monorepo root
# https://docs.railway.app/config-as-code/reference

[build]
builder = "NIXPACKS"
buildCommand = "bun install && bun run build:nextjs"

[deploy]
startCommand = "bun run start:nextjs"
