[build]
  # Build from repo root so workspace deps install correctly; Next.js app lives in apps/nextjs
  command = "bun install && bun run build --filter=@saasfly/nextjs"
  publish = ".netlify"

[build.environment]
  NODE_VERSION = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs"
